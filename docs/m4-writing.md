# M4 PR4: structured writing

Writing output is untrusted input. The post draft contract requires title, body, source story provenance, prompt version and claim-level evidence status. Invalid output is rejected before it can enter content state; provider retry/fallback orchestration remains behind the AI boundary.
